#include<stdio.h>
int main(){
    int i=0;
    while(i++){
        printf("loop");
        if(i==3)
        break;
    }
    return 0;
}
// print nothing while(i++) therefore while(0)=false hence nothing